pub(crate) struct RecursionType {
    strenum: StrEnum,
    ELEMENTS_FIRST: ,
    FLATTEN: ,
    ELEMENTS_ONLY: ,
}

impl RecursionType {
    pub(crate) fn new() -> RecursionType {
        RecursionType {
            strenum: StrEnum::new(),
            ELEMENTS_FIRST: todo!(),
            FLATTEN: todo!(),
            ELEMENTS_ONLY: todo!(),
        }
    }
    
}