pub(crate) struct Voice {
    stream: Stream,
    recursionType: ,
    classSortOrder: ,
}

impl Voice {
    pub(crate) fn new() -> Voice {
        Voice {
            stream: Stream::new(),
            recursionType: todo!(),
            classSortOrder: todo!(),
        }
    }
    
}