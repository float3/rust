pub(crate) struct System {
    stream: Stream,
    systemNumber: ,
    systemNumbering: ,
}

impl System {
    pub(crate) fn new() -> System {
        System {
            stream: Stream::new(),
            systemNumber: todo!(),
            systemNumbering: todo!(),
        }
    }
    
}