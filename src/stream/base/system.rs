pub struct System {
    stream: Stream,
    systemNumber: ,
    systemNumbering: ,
}

impl System {
    pub fn new() -> System {
        System {
            stream: Stream::new(),
            systemNumber: todo!(),
            systemNumbering: todo!(),
        }
    }
    
}