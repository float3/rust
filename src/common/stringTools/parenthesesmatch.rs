pub(crate) struct ParenthesesMatch {
    start: IntegerType,
    end: IntegerType,
    text: String,
    nested: ,
}

impl ParenthesesMatch {
    pub(crate) fn new() -> ParenthesesMatch {
        ParenthesesMatch {
            start: todo!(),
            end: todo!(),
            text: todo!(),
            nested: todo!(),
        }
    }
    
}