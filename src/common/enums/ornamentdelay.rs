pub(crate) struct OrnamentDelay {
    strenum: StrEnum,
    NO_DELAY: ,
    DEFAULT_DELAY: ,
}

impl OrnamentDelay {
    pub(crate) fn new() -> OrnamentDelay {
        OrnamentDelay {
            strenum: StrEnum::new(),
            NO_DELAY: todo!(),
            DEFAULT_DELAY: todo!(),
        }
    }
    
}