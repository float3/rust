pub(crate) struct Accordion {
    organ: Organ,
}

impl Accordion {
    pub(crate) fn new() -> Accordion {
        Accordion {
            organ: Organ::new(),
        }
    }

    pub(crate) fn new(&self) {
        todo!()
    }
}
