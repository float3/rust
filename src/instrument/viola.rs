pub(crate) struct Viola {
    stringinstrument: StringInstrument,
}

impl Viola {
    pub(crate) fn new() -> Viola {
        Viola {
            stringinstrument: StringInstrument::new(),
        }
    }

    pub(crate) fn new(&self) {
        todo!()
    }
}
